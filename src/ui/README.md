# UI module

This directory owns menus, overlays, HUD shell wiring, and future storefront presentation code.

Current state: the legacy menu remains in `index.html` and `launch.js`; new commercial UI code should be added here behind explicit adapters.
